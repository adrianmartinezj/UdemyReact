// Have to name Auxiliary.js instead of Aux.js because the latter
// is reserved in Windows.

// Don't even need this because we're not doing
// anything react specific.

// import React from 'react';

const aux = props => props.children;

export default aux;